import 'package:flutter/material.dart';

class MealsInitialView extends StatelessWidget {
  const MealsInitialView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
